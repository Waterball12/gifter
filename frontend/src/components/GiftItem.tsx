import React from 'react';

const GiftItem = ({
                      move, swap, push, insert, unshift, pop, form
                  }) => {
    return (
        <div>
            
        </div>
    );
};

export default GiftItem;